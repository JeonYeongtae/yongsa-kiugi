# 용사키우기 — 수정 작업 리스트

> 작업 순서: 번호 순서대로 진행 (각 항목 완료 후 체크)

---

## 프롤로그 화면 수정

### [x] 1. 챕터 전환 시 화면 전환 방식으로 변경

**현재**: 스크롤 피드 방식으로 챕터 헤더가 아래에 추가됨  
**변경**: 챕터(장) 단위로 화면 전체를 교체하는 슬라이드/페이드 전환

**추가 로직 필요:**
- **이전 버튼**: 이전 챕터(섹션)로 돌아가 해당 스토리 내용 재확인 가능
- **Log 버튼**: 진행된 챕터별 스토리 요약 목록 제공 (현재 CLAUDE.md에서 Log 버튼 없음으로 명시됨 → 이번 변경으로 추가)

---

### [x] 2. 타이핑 애니메이션 효과 추가

**현재**: 텍스트 블록이 탭 시 즉시 전체 출력  
**변경**: 타이핑 효과로 한 글자씩 출력

**세부 사항:**
- 타이핑 효과가 끝난 시점에 화살표 아래(`▼`) 버튼 표시 (계속 진행 유도)
- 타이핑 진행 중 탭(터치) 1회 → 즉시 전체 텍스트 표시 (애니메이션 생략)

---

### [x] 3. 하단 장면 개수 카운터 제거

**현재**: 하단 컨트롤 바에 `N / M 장면` 표시  
**변경**: 해당 UI 요소 제거

---

### [x] 4. x2 속도 버튼 추가

**현재**: 없음  
**변경**: 타이핑 애니메이션 속도를 2배로 빠르게 하는 `×2` 토글 버튼 추가  
- 위치: 하단 컨트롤 바 (Skip 버튼 옆 등)
- 토글 상태 유지 (켜면 이후 모든 블록에 적용)

> 작업 2(타이핑 효과) 완료 후 진행

---

### [x] 5. 기질 선택 화면 레이아웃 오류 수정

**위치**: `MbtiBirthdayModal` → `step === 0` (기질 선택 단계)  
**현재**: 스마트폰 크기에 따라 버튼/설명 컴포넌트 규격이 틀어짐  
**변경**:
- 기질 버튼 크기 고정, 글자는 반응형으로 조절 (버튼 넘침 방지)
- 기질 설명 카드: 현재 크기를 고정 높이 카드로 설정, 내용이 길면 스크롤로 표시

---

### [x] 6. 생일 선택 화면 수정

**위치**: `MbtiBirthdayModal` → `step === 1` (생일 선택 단계)  
**현재**: 월/일 토글에 기본 선택 없음, 글자 크기 작음  
**변경**:
- 기본값: 1월 1일 선택 상태로 초기화 (`month: 1`, `day: 1`)
- 토글(select) 내부 월/일 레이블 텍스트 제거 (`-- 월`, `-- 일` placeholder 등)
- 날짜 글자 크기 확대

---

---

## 메인 허브 화면 수정

### [x] 7. "전대용사" 표시를 "당신"으로 수정

**위치**: `MainHubScreen.jsx` line 14  
**현재**: `▸ 전대용사`  
**변경**: `▸ 당신`

---

### [x] 8. 스케줄 시작 방식 선택 모달 추가

**위치**: `MainHubScreen.jsx` → 스케줄 시작 버튼 클릭 시  
**현재**: 클릭 시 바로 `WeeklyScheduleScreen`(월간 일괄 배분)으로 이동  
**변경**: 먼저 모달 표시 후 방식 선택

**모달 선택지:**
- **월간 처리**: 4주차 슬롯을 한 번에 배분 (현재 구현 방식) → `weekly-schedule`으로 이동
- **주간 처리**: 1주 슬롯만 배분 후 바로 진행 → 1주 단위 배분 화면으로 이동

> 주간 처리 방식은 `WeeklyScheduleScreen` 내 1주차만 표시하는 모드 분기로 구현 가능

---

## 스케줄 화면 수정

### [x] 9. SchedulePickModal — 등급 필터 → 스탯 필터로 변경

**위치**: `modals/SchedulePickModal.jsx`  
**현재**: `초급 / 중급 / 상급` 탭으로 필터링  
**변경**: 스탯 기반 필터링 (예: 물공 / 마공 / 물방 / 마방 / 명중 / 회피 / 마나 / 스태미나 등)

**일정 나열 방식 변경:**
- 종류별로 묶어서 `초급 → 중급 → 상급` 순 배치
  - 예: `[광산 채굴 초급] [광산 채굴 중급] [광산 채굴 상급] ── [성벽 야간 경비 초급] ...`
- 종류 그룹 사이에 구분선 추가

---

### [x] 10. 주간 보고 제거 — 월간 보고만 유지

**현재**: `WeeklyReportScreen` → 4주 완료 시 `MonthlyReportScreen`  
**변경**: `WeeklyReportScreen` 화면 제거, 주차 완료 시 바로 다음 주 스케줄 배분으로 이동 (4주 완료 시 월간 보고)

**세부 사항:**
- `WeeklyReportScreen.jsx` 비활성화(또는 라우팅에서 제거)
- `ScheduleExecScreen`의 주차 완료 CTA:
  - 주차 < 4: `다음 주차로 ▸` → 다음 주 스케줄 배분 화면
  - 주차 === 4: `월간 보고 ▸` → `MonthlyReportScreen`

**MonthlyReportScreen 수정:**
- `주차별 활동 로그` 섹션 제거
- `이달의 성과` 각 숫자(PERFECT / GOOD / BAD / 총 활동 수) 탭 시 상세 모달 오픈
  - 모달 내용: 해당 등급에 해당하는 일정 목록 (주차 + 슬롯명 + 결과)

---

---

## 편성 화면 수정

> 대상: `game-ui/src/screens/FormationScreen.jsx`

### [x] 11. 헤더바 높이 줄이기

**현재**: 표준 `<Header />` 컴포넌트 사용 (높이 20%)  
**변경**: 편성 화면 전용으로 헤더 높이 축소

> `Header.jsx`를 직접 수정하면 전체 화면에 영향 → FormationScreen에서 Header를 제거하고 자체 타이틀 바로 교체하거나, Header에 prop으로 높이 변형 추가하는 방식 검토

---

### [x] 12. "세팅" → "프리셋" 리네이밍 및 UI 전면 교체

**현재**: 화면 상단에 `PresetBar` 가로 고정 노출 → 슬롯 탭 시 `PresetModal` 오픈

**추천 방식 (세로형 모바일 RPG 표준 패턴):**
- `PresetBar` 제거, 타이틀 바 우측에 `프리셋 ≡` 아이콘 버튼 배치
- 탭 시 하단에서 슬라이드 업 되는 바텀시트 형태로 3개 슬롯을 **세로 카드 리스트**로 표시
- 각 슬롯 카드 구성 (1행):
  - 좌측: `프리셋 N` 번호 배지
  - 중앙: 저장된 로드아웃 미니 요약 (무기 아이콘 + 스킬 2칸 + 파티원 3칸 도트)
  - 우측: 빈 슬롯이면 `저장 ▸` 단독 / 채워진 슬롯이면 `불러오기 ▸` + 우측 `⋯` 탭 시 덮어쓰기·삭제 인라인 표시
- 바텀시트 외부 탭 또는 상단 `✕`로 닫기

> `PresetBar` 컴포넌트 제거, `PresetModal`은 슬롯별 바텀시트로 교체  
> 타이틀 바 우측 버튼과 바텀시트 열림/닫힘 상태를 `useState`로 관리

---

### [x] 13. 스킬/파티원 서브뷰 헤더 우측 편성 현황 제거

**현재**:
- `SkillView` 헤더 우측: `장착 N/2` 표시 (`FormationScreen.jsx:360`)
- `PartyView` 헤더 우측: `파티 N/3` 표시

**변경**: 양쪽 헤더에서 해당 카운터 UI 요소 제거

---

### [x] 14. 파티원 편성 목록 필터링 — 역할 → 무기군 기준으로 변경

**현재**: `ROLE_FILTERS = ['전체', '딜러', '원거딜', '마법사', '탱커', '힐러', '버프', '디버프']`  
**변경**: 무기군 기준으로 간략화

**무기군 분류** (COMPANIONS 데이터의 `weapon` 필드 기준):
- `전체 / 근접 / 방어 / 마법 / 원거 / 서포`

> `FormationScreen.jsx` 내 `ROLE_FILTERS` 상수 및 `roleFilter` 비교 로직 수정  
> `c.role === roleFilter` → `c.weapon === weaponFilter` 로 변경

---

---

## 메인 화면 구성 수정

> 대상: `Nav.jsx`, `Header.jsx`, `MainHubScreen.jsx`, `EncyclopediaScreen.jsx`, `FormationScreen.jsx`

### [x] 15. 헤더바 스탯 버튼 → 네비바 '스탯' 탭으로 이동

**현재**: `Header.jsx`에서 좌측 버튼으로 `stats` 화면 이동  
**변경**:
- `Nav.jsx`에 `{ id: 'stats', label: '스탯', screen: 'stats' }` 탭 추가
- `ACTIVE_MAP`에 `'stats': 'stats'` 추가
- `Header.jsx`에서 스탯 이동 버튼 제거

---

### [x] 16. 네비바 구조 개편 — 오버레이 페이지 개념 정립

**현재**: 네비바 탭 클릭 시 전체 화면 전환  
**변경 방향**: 허브가 항상 베이스레이어, 다른 탭은 그 위에 오버레이되는 느낌

**구체 내용:**
- 모든 화면에서 뒤로(`‹`) 이동 시 `main-hub`로 복귀
- 허브 탭은 별도 탭으로 유지 (또는 허브를 홈 아이콘으로 표현)
- `ACTIVE_MAP`에서 모든 서브화면(`weekly-schedule`, `schedule-exec` 등)이 `hub`로 매핑되는 현재 방식 유지 및 확장

> 현재 라우팅 구조(`GameContext.navigate`)는 단순 상태 전환이므로 실제 스택 히스토리는 없음  
> 느낌을 주는 방향으로 전환 애니메이션 또는 back 버튼 로직 정리 필요

---

### [x] 17. 인벤토리 네비바 탭 추가 및 구현

**현재**: 인벤토리(`InventoryScreen.jsx`) 파일 존재하나 네비바에 없음  
**변경**:
- `Nav.jsx`에 `{ id: 'inventory', label: '인벤', screen: 'inventory' }` 탭 추가
- `InventoryScreen.jsx` 구현 확장: **장비 아이템 + 사용 아이템** 탭 분리
  - 사용 아이템 분류: 배틀 아이템, 선물 아이템 등
  - 아이템 데이터: **노션 8번 문서 확인 후 반영 필요**

> 작업 시작 전 노션 8번 문서 내용 공유 필요

---

### [x] 18. 도감 — 캐릭터 상세 페이지 수정

**위치**: `EncyclopediaScreen.jsx`

**세부 변경:**

1. **대화 기록 탭 제거**
   - 현재 `[['affinity', '호감도 기록'], ['dialog', '대화 기록']]` 탭 2개 표시 (line 648)
   - `dialog` 탭 제거 → `호감도 기록` 탭만 유지 (탭 UI 자체도 불필요하면 제거)

2. **호감도 기록 내용 변경**
   - 현재: 호감도 변동 로그 전체 표시
   - 변경: **스토리 조우** 로그 + **선물 기록** 로그만 표시 (일반 대화 로그 제외)

3. **프로필 포지션 표시 → 무기 아이콘으로 수정**
   - 현재: 역할(딜러/탱커 등) 텍스트 표시
   - 변경: 해당 캐릭터의 무기군 아이콘으로 대체 (`CAT_ICON[companion.weapon]` 활용)

4. **여관 체류 표시 범위 제한**
   - 현재: 모든 캐릭터에 여관 체류 여부 배지 표시 (`staying: true/false`)
   - 변경: **`staying: true`인 캐릭터에만** 여관 배지 표시 (비체류 캐릭터는 배지 없음)

5. **호감도 수치 → 하트 3개 UI로 변경**
   - 현재: `호감도 N/100` 숫자 텍스트
   - 변경: 하트 3개 표시, 각 하트는 30 단위로 채워짐
     - 0~29: 하트 0개 채움 / 30~59: 1개 / 60~89: 2개 / 90+: 3개 (또는 각 하트 내 부분 채움)

---

### [x] 19. 편성 — 파티원 목록을 여관+파티 5명으로 제한

**현재**: `COMPANIONS` 전체 목록(12명) 표시  
**변경**: **파티 편성 3명 + 여관 체류 2명 = 최대 5명**만 동료 목록에 표시

**로직:**
- 동료 목록 필터: `staying: true` 캐릭터 + `party`에 포함된 캐릭터만 표시
- 여관 슬롯은 최대 2개 (도감 여관 관리와 연동 필요)

> `FormationScreen.jsx`의 `COMPANIONS.filter()` 로직 수정  
> 여관 체류 상태는 `GameContext` 또는 `EncyclopediaScreen`의 `staying` 상태와 공유 필요

---

---

## 탐험 / 지도 화면 수정

> 대상: `WorldMapScreen.jsx`, `BattleMainScreen.jsx`, 신규 `ExplorationActionScreen.jsx`

### [x] 20. 지도 화면 — 하단 설명 제거, 지역 탭 시 바로 이동

**현재**: 지도(68%) + 하단 선택 지역 설명 패널(32%) 구조. 진입 버튼 별도 존재  
**변경**: 지도 전체화면 표시. 지역 노드 탭 시 즉시 해당 지역으로 진입

**세부:**
- 하단 설명 패널(`bg-slate-100` 텍스트 영역) 제거
- 지도 영역을 `flex-1` 전체로 확장
- 노드 `onClick` → 설명 표시 없이 바로 `ExplorationActionScreen` (또는 해당 지역 화면)으로 이동
- 행동력 소모는 이 시점이 아닌 행동 선택 이후 적용

---

### [x] 21. 지역 진입 후 행동 선택 화면 신규 구현

**신규**: `ExplorationActionScreen.jsx` (또는 기존 `EventChoiceScreen` 확장)

**화면 구성:**
```
[지역명] (예: 엘프 유적)   ← 타이틀 바
이동됐어. 무엇을 할까?     ← 본문 텍스트

1. 사람을 만날래   (행동력 1)  → 동료 영입 이벤트 (아이템 보유 시 프리패스 문구 표시)
2. 물건을 찾아볼래 (행동력 1)  → 아이템 or 무기 획득 이벤트
3. 그냥 돌아다닐래 (행동력 1)  → 특성 or 스탯 획득 이벤트
4. [조건부] 토벌가자 / 특수 상황  ← 특정 조건 달성 시에만 표시
```

**행동력 소모**: 선택지 탭 시 행동력 차감 → 각 이벤트 화면으로 이동

---

### [x] 22. 전투 화면 — 헤더 턴카드 UI 구현

**현재**: `BattleMainScreen` 헤더에 `<Header />` 컴포넌트 사용  
**변경**: 헤더 영역을 **속도 기반 턴 배분 카드 열**로 교체

**턴카드 설계:**
- 좌→우로 나열된 캐릭터 아이콘 카드 (예: `[적] [아] [적] [아] [아] [적] [아]`)
- 스피드가 높은 유닛일수록 턴 카드가 더 많이 배분
- 현재 행동할 유닛의 카드를 강조 (amber 테두리 등)
- 카드 스크롤 가능 (턴이 긴 경우 가로 스크롤)

```
┌────────────────────────────────────────┐
│ [고블] [HERO] [고블] [HERO] [아딘] ...  │  ← 헤더 턴카드 열
└────────────────────────────────────────┘
```

> `Header` 컴포넌트 대신 자체 `TurnBar` 컴포넌트로 교체

---

### [x] 23. 전투 화면 — 아군/적군 각 최대 4명 배치 UI

**현재**: 아군 2명, 적군 2명 하드코딩 배치  
**변경**: 아군 최대 4명 + 적군 최대 4명 슬롯으로 UI 확장

**배치 영역 재구성:**
- 좌측: 아군 4슬롯 (파티 3명 + 여관 1명, 또는 전투 참여 인원)
- 우측: 적군 4슬롯
- 슬롯 크기 조정 (현재 `w-7 h-7` → 4명 배치에 맞게 균등 분배)
- HP/MP 바 유지

---

### [x] 24. 전투 로그 — 투명 그라데이션 배경 + 아래서 올라오는 로그

**현재**: `bg-slate-100` 고정 배경의 정적 텍스트 로그 (`BattleMainScreen.jsx:38`)  
**변경**:

**배경 처리:**
- 하단 불투명(`bg-slate-100/90`) → 위로 갈수록 서서히 투명해지는 그라데이션
  - Tailwind: `bg-gradient-to-t from-slate-100/90 via-slate-100/50 to-transparent`

**로그 동작:**
- 최신 로그가 아래서 올라오는 방식 (새 로그 → 하단에 append, 위로 push)
- 전투 진행에 따라 오래된 로그가 로그 영역 상단을 벗어나면 `opacity-0` 트랜지션으로 스르륵 사라짐
- `overflow-hidden` + `useEffect` 스크롤 자동 유지

---

### [x] 25. 지도 개편 — 대권역 슬라이드 + 해금 상태 표시 + 공용 지역 고정

**현재**: 5개 노드가 단일 지도에 고정 배치  
**변경**:

**대권역 데이터 (노션 기준):**

| 대권역 | 지역 1 (순서 1) | 지역 2 (순서 2) | 지역 3 (순서 3) |
|---|---|---|---|
| 엘프의 숲 | 엘프 마을 | 정령의 샘 | 고대 유적 |
| 왕성 | 번화하는 왕성 | 외곽 뒷골목 | 지하 불법 투기장 |
| 드워프 광산 | 길드 | 용암이 끓는 지하 대협곡 | 대금광 |
| 수인 부락 | 열대 우림 | 대산맥 | 작열하는 대사막 |
| 마족 영지 | 백은 설원 | 잿빛 경계지역 | 심연 회랑 |

**특수 지역 (슬라이드 외 고정):**

| 지역 | 대권역 | 열리는 순서 | 비고 |
|---|---|---|---|
| 태초의 숲 | 태초의 숲 | 0 | 항상 개방 (프롤로그 배경) |
| 마왕군 사령기지 | 마왕성 | 3 or 4 | 후반 조건부 해금 |
| 마왕성 | 마왕성 | 5 | 최후반 |
| 초기 사냥터 | 사냥터 | 1 | 공용 지역 |
| 중기 사냥터 | 사냥터 | 2 | 공용 지역 |
| 후기 사냥터 | 사냥터 | 3 | 공용 지역 |

**지도 구조:**
- 대권역 5개를 `‹ ›` 화살표로 슬라이드
- 각 대권역 페이지에 지역 3개 노드 배치
- 우측 상단: 현재 대권역 이름 표시 (예: `🌲 엘프의 숲`)
- **고정 표시**: 화면 하단 또는 별도 영역에 공용 지역(사냥터) + 태초의 숲 항상 노출

**해금 상태 표시 (경우의 수 모두 시각화):**
- 완전 해금: 노드 정상 표시, 탭 가능
- 완전 잠김: `🔒` 아이콘, 회색/비활성
- 일부 해금: 대권역 안에서 열린 지역만 활성, 잠긴 지역은 `🔒`
- 조건부(3 or 4): 별도 조건 아이콘 표시

```
[‹]  [엘프 마을 ✓] [정령의 샘 ✓] [고대 유적 🔒]  [›]    🌲 엘프의 숲
──────────────────────────────────────────────────
[태초의 숲]  [초기 사냥터] [중기 사냥터 🔒] [후기 🔒]  ← 공용 고정
```

> 슬라이드: `useState(regionIdx)` + 조건 렌더링  
> 해금 상태: `GameContext` 또는 로컬 상수 배열로 관리

---

## 메모

- CLAUDE.md 3절 내러티브 화면 기준: `Log 버튼 없음`으로 명시됨 → 작업 1 완료 후 CLAUDE.md 해당 항목 업데이트 필요
- 작업 2, 4는 묶어서 구현 (타이핑 로직 공유)
- 작업 5, 6은 `MbtiBirthdayModal` 내부 수정 (동일 컴포넌트)
- 작업 8의 주간 처리 방식은 작업 10(주간 보고 제거)과 연관 — 함께 설계 필요
- 작업 11에서 Header 수정 방식 확정 필요 (전체 영향 vs 편성 전용 타이틀 바)
- 작업 17: 노션 8번 문서 공유 필요 (인벤토리 아이템 데이터)
- 작업 19: 여관 체류 상태 전역 관리 필요 (`GameContext`에 `innMembers` 추가 검토)
- 작업 21: 선택지 분기 후 각 이벤트 결과 화면 별도 설계 필요 (동료 영입 / 아이템 획득 / 특성 획득)
- 작업 22: 배틀 로직 미확정 — 예시 수준 UI로만 구현 (실제 speed 계산 로직은 추후 설계)
- 작업 25: 지역 데이터 확정됨 (노션 기준). 마왕성/사냥터 해금 조건 세부 확정 필요

---

## 첫 화면 / 공통 UI 수정

> 대상: `MainMenuScreen.jsx`, 전체 모달 파일, `BattleMainScreen.jsx`

### [x] 26. 첫 화면 도감 — 모든 세이브 파일 통합 컬렉션으로 변경

**현재**: `MainMenuScreen`에서 `▸ 도감` 버튼 → `EncyclopediaModal` 오픈 (현재 회차 캐릭터 정보)  
**변경**: 전체 세이브 파일을 아우르는 **마스터 컬렉션 도감**으로 재구성

**컬렉션 카테고리 (목록화):**
- 파티원 (수집한 동료 캐릭터 전체)
- 장비 (획득한 무기/아이템 전체)
- 엔딩 (달성한 엔딩 목록)
- 기타 수집 정보 (특성, 지역 방문 등)

**화면 구성:**
- 카테고리별 탭 또는 섹션
- 각 항목: 수집 완료 / 미수집 표시 (미수집 항목은 `???` 또는 실루엣)
- 세이브 파일과 무관하게 누적 기록 (별도 저장소 필요)

> 현재 `EncyclopediaModal`(`modals/EncyclopediaModal.jsx`)은 인게임 도감과 별개로 유지  
> `MainMenuScreen`의 `▸ 도감` 버튼은 이 새로운 컬렉션 화면으로 연결

---

### [x] 27. 전체 모달 — X 닫기 버튼 위치 좌측 → 우측으로 변경

**현재**: 모달 좌측 상단 `✕` 버튼 (`absolute top-2 left-2`) — CLAUDE.md 3절 기준  
**변경**: 우측 상단 `✕` 버튼 (`absolute top-2 right-2`)

**영향 범위**: `src` 전체에서 `absolute top-2 left-2` 패턴 17개소 일괄 수정  
**CLAUDE.md 3절도 함께 업데이트 필요**

> `replace_all` 방식 또는 파일별 일괄 수정

---

### [x] 28. 전투 화면 — 배치된 캐릭터 위에 HP/MP 바 표시

**현재**: 캐릭터 카드 아래에 `bg-red-700` / `bg-blue-700` 바가 카드와 분리되어 표시  
**변경**: 캐릭터 카드 이미지 **위에 오버레이**되는 형태로 HP/MP 바 재배치

**디자인:**
- 캐릭터 카드를 `relative` 컨테이너로 감싸기
- HP 바: 카드 하단 내부 `absolute bottom-0` 빨간 바
- MP 바: HP 바 위 또는 카드 상단 내부 파란 바
- 바 위에 수치 텍스트 선택적 표시 (`text-[6px]`)

---

## 메모

- CLAUDE.md 3절 내러티브 화면 기준: `Log 버튼 없음`으로 명시됨 → 작업 1 완료 후 CLAUDE.md 해당 항목 업데이트 필요
- 작업 2, 4는 묶어서 구현 (타이핑 로직 공유)
- 작업 5, 6은 `MbtiBirthdayModal` 내부 수정 (동일 컴포넌트)
- 작업 8의 주간 처리 방식은 작업 10(주간 보고 제거)과 연관 — 함께 설계 필요
- 작업 11에서 Header 수정 방식 확정 필요 (전체 영향 vs 편성 전용 타이틀 바)
- 작업 17: 노션 8번 문서 공유 필요 (인벤토리 아이템 데이터)
- 작업 19: 여관 체류 상태 전역 관리 필요 (`GameContext`에 `innMembers` 추가 검토)
- 작업 21: 선택지 분기 후 각 이벤트 결과 화면 별도 설계 필요 (동료 영입 / 아이템 획득 / 특성 획득)
- 작업 22: 배틀 로직 미확정 — 예시 수준 UI로만 구현 (실제 speed 계산 로직은 추후 설계)
- 작업 25: 지역 데이터 확정됨 (노션 기준). 마왕성/사냥터 해금 조건 세부 확정 필요
- 작업 27: X 버튼 우측 이동은 CLAUDE.md 기존 규칙과 충돌 → CLAUDE.md 동시 업데이트 필요
- 작업 26: 세이브 파일 간 누적 컬렉션 저장 방식 확정 필요 (localStorage 별도 키 등)
- 추가 항목 있을 예정 (사용자 이어서 나열 중)
